<div class="main">
    <h1>Hello World</h1>
    
    {{#if session.isAuthenticated}}
        <button {{ action 'invalidateSession' }}>Logout</button>
    {{else}}
        <button {{ action 'authenticateSession' }}>Login</button>
    {{/if}}
    
    {{outlet}}
</div>